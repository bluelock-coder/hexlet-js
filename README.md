# Hexlet JS Project

## Команды

`make install` — устанавливает все зависимости проекта (пакеты из package.json).
## Команды

* `make lint` — запуск проверки кода линтером.
* `make lint-fix` — автоматическое исправление доступных ошибок линтинга.
